apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-ui
    app.kubernetes.io/instance: nginx-ui
  name: nginx-ui
  namespace: product-material-pass
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: nginx-ui