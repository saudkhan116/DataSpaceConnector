apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edc-consumer.fullname" . }}-test-connection"
  labels:
    {{- include "edc-consumer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edc-consumer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
